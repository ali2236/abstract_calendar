import 'package:abstarct_date/abstarct_date.dart';
import 'package:flutter/material.dart';

class AbstractCalendar<T extends DateAdapter> extends StatefulWidget {
  @override
  _AbstractCalendarState createState() => _AbstractCalendarState();
}

class _AbstractCalendarState extends State<AbstractCalendar> {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[

      ],
    );
  }
}
